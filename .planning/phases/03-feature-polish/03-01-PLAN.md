---
phase: 03-feature-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - JobHarvest/Utils/Extensions.swift
  - JobHarvest/Views/Shared/LoadingView.swift
  - JobHarvest/Views/Shared/ErrorBannerView.swift
  - JobHarvest/Models/Job.swift
autonomous: true
requirements: [UX-01, UX-02, UX-04, SWIPE-06]

must_haves:
  truths:
    - "Error.humanReadableDescription maps raw SDK strings to user-friendly messages"
    - "BrandedLoadingView shows a pulsing bolt icon instead of generic ProgressView"
    - "ErrorBannerView displays inline error with optional retry button"
    - "JobFilters conforms to Equatable for context-aware empty deck comparison"
  artifacts:
    - path: "JobHarvest/Utils/Extensions.swift"
      provides: "Error.humanReadableDescription computed property"
      contains: "var humanReadableDescription"
    - path: "JobHarvest/Views/Shared/LoadingView.swift"
      provides: "Branded logo pulse animation replacing ProgressView"
      contains: "bolt.fill"
    - path: "JobHarvest/Views/Shared/ErrorBannerView.swift"
      provides: "Reusable inline error banner with retry"
      contains: "struct ErrorBannerView"
    - path: "JobHarvest/Models/Job.swift"
      provides: "JobFilters Equatable conformance"
      contains: "Equatable"
  key_links:
    - from: "JobHarvest/Utils/Extensions.swift"
      to: "All ViewModels"
      via: "error.humanReadableDescription replaces error.localizedDescription"
      pattern: "humanReadableDescription"
---

<objective>
Create shared UX foundation components that all subsequent plans depend on: human-readable error mapping, branded loading animation, reusable error banner, and JobFilters Equatable conformance.

Purpose: These cross-cutting patterns are consumed by Plans 02-05. Building them first avoids duplication and ensures consistent UX from the start.
Output: Four modified/created files providing reusable patterns for the entire phase.
</objective>

<execution_context>
@/Users/williamwriter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/williamwriter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-polish/03-CONTEXT.md
@.planning/phases/03-feature-polish/03-RESEARCH.md
@.planning/phases/03-feature-polish/03-UI-SPEC.md

<interfaces>
<!-- Existing error pattern in all ViewModels -->
From all ViewModels:
```swift
@Published var error: String?
// Set via: self.error = error.localizedDescription
```

From JobHarvest/Models/Job.swift:
```swift
struct JobFilters: Codable {
    var jobIndustry: String?
    var locationState: String?
    var jobType: [String]?
    var minimumSalary: Double?
    var postedAfterTimestamp: String?
    var companyKey: String?
    var fromDB: Bool?
}
```

From JobHarvest/Views/Shared/LoadingView.swift:
```swift
// Current: uses generic ProgressView()
struct LoadingView: View { ... }
```

From JobHarvest/Utils/Constants.swift:
```swift
extension Color {
    static let flashTeal = Color(hex: "#1abc9c")
    static let flashNavy = Color(hex: "#1a237e")
    static let flashDark = Color(hex: "#2c3e50")
    static let flashTextSecondary = Color(hex: "#7f8c8d")
    static let flashOrange = Color(hex: "#e67e22")
    static let flashBackground = Color(hex: "#F5F5F5")
    static let flashWhite = Color(hex: "#FAFAFA")
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Error humanization extension and branded loading animation</name>
  <files>JobHarvest/Utils/Extensions.swift, JobHarvest/Views/Shared/LoadingView.swift</files>
  <read_first>
    - JobHarvest/Utils/Extensions.swift
    - JobHarvest/Views/Shared/LoadingView.swift
    - JobHarvest/Utils/Constants.swift
    - .planning/phases/03-feature-polish/03-UI-SPEC.md (lines 200-225 for error mapping table and loading spec)
  </read_first>
  <action>
**In `Extensions.swift`**, add a computed property on `Error`:

```swift
extension Error {
    var humanReadableDescription: String {
        let raw = localizedDescription
        // Network errors
        if raw.contains("URLError") || raw.contains("-1009") || raw.contains("NSURLErrorDomain") {
            return "No internet connection. Check your connection and retry."
        }
        if raw.contains("-1001") {
            return "The request timed out. Please try again."
        }
        // HTTP status codes
        if raw.contains("403") || raw.contains("Forbidden") {
            return "You've reached your daily limit. Upgrade to continue."
        }
        if raw.contains("401") || raw.contains("Unauthorized") {
            return "Your session has expired. Please sign in again."
        }
        if raw.contains("404") || raw.contains("Not Found") {
            return "This content is no longer available."
        }
        if raw.contains("500") || raw.contains("Internal Server Error") {
            return "Something went wrong on our end. Please try again."
        }
        // AWS/Cognito errors
        if raw.contains("UserNotConfirmedException") {
            return "Please verify your email before signing in."
        }
        if raw.contains("NotAuthorizedException") {
            return "Incorrect email or password. Please try again."
        }
        if raw.contains("UsernameExistsException") {
            return "An account with this email already exists."
        }
        if raw.contains("AWSCognito") || raw.contains("Cognito") {
            return "Authentication error. Please sign out and sign in again."
        }
        // Default
        return "An unexpected error occurred. Please try again."
    }
}
```

Do NOT remove any existing code in Extensions.swift. Append this extension at the end of the file.

**In `LoadingView.swift`**, replace the current `ProgressView()` body with a branded pulsing bolt animation:

```swift
struct LoadingView: View {
    var message: String = "Loading..."
    @State private var isAnimating = false

    var body: some View {
        VStack(spacing: 20) {
            Image(systemName: "bolt.fill")
                .font(.system(size: 48, weight: .bold))
                .foregroundStyle(
                    LinearGradient(
                        colors: [.flashTeal, .flashNavy],
                        startPoint: .top,
                        endPoint: .bottom
                    )
                )
                .scaleEffect(isAnimating ? 1.15 : 0.95)
                .opacity(isAnimating ? 1.0 : 0.6)
                .animation(
                    .easeInOut(duration: 0.8).repeatForever(autoreverses: true),
                    value: isAnimating
                )
                .onAppear { isAnimating = true }

            Text(message)
                .font(.system(size: 14, weight: .regular))
                .foregroundColor(.flashTextSecondary)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Color.flashBackground)
    }
}
```

Preserve the same `message` parameter name and default. If `LoadingView` currently accepts `message:` as an init parameter, keep the same signature. Check `Assets.xcassets` for a FlashApply logo image first -- if one exists (e.g., "FlashApplyLogo" or "AppIcon"), use `Image("FlashApplyLogo")` instead of `Image(systemName: "bolt.fill")`. If no logo asset exists, use `bolt.fill`.
  </action>
  <verify>
    <automated>cd "/Users/williamwriter/Desktop/Jake's Project/FlashApply-iOS" && xcodebuild build -project JobHarvest/JobHarvest.xcodeproj -scheme JobHarvest -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Extensions.swift contains `var humanReadableDescription: String`
    - Extensions.swift contains `"No internet connection. Check your connection and retry."`
    - Extensions.swift contains `"You've reached your daily limit. Upgrade to continue."`
    - Extensions.swift contains `"An unexpected error occurred. Please try again."`
    - LoadingView.swift contains `bolt.fill` or `FlashApplyLogo`
    - LoadingView.swift contains `.scaleEffect(isAnimating`
    - LoadingView.swift contains `.repeatForever(autoreverses: true)`
    - LoadingView.swift contains `flashTeal`
    - Build succeeds with zero errors
  </acceptance_criteria>
  <done>Error.humanReadableDescription maps all raw SDK patterns from UI-SPEC table to user-friendly strings. LoadingView shows branded pulse animation instead of generic ProgressView.</done>
</task>

<task type="auto">
  <name>Task 2: ErrorBannerView component and JobFilters Equatable</name>
  <files>JobHarvest/Views/Shared/ErrorBannerView.swift, JobHarvest/Models/Job.swift</files>
  <read_first>
    - JobHarvest/Models/Job.swift
    - JobHarvest/Views/Shared/ErrorView.swift
    - JobHarvest/Utils/Constants.swift
    - .planning/phases/03-feature-polish/03-UI-SPEC.md (lines 206-211 for error banner spec)
  </read_first>
  <action>
**Create `JobHarvest/Views/Shared/ErrorBannerView.swift`** (new file):

```swift
import SwiftUI

struct ErrorBannerView: View {
    let message: String
    var onRetry: (() -> Void)?

    var body: some View {
        HStack(spacing: 12) {
            Image(systemName: "exclamationmark.triangle.fill")
                .foregroundColor(.flashOrange)
            Text(message)
                .font(.system(size: 14))
                .foregroundColor(.flashDark)
                .lineLimit(2)
            Spacer()
            if let retry = onRetry {
                Button("Retry", action: retry)
                    .font(.system(size: 14, weight: .bold))
                    .foregroundColor(.flashTeal)
            }
        }
        .padding(14)
        .background(Color(hex: "#e74c3c").opacity(0.10))
        .overlay(
            RoundedRectangle(cornerRadius: 10)
                .stroke(Color(hex: "#e74c3c").opacity(0.3), lineWidth: 1)
        )
        .cornerRadius(10)
        .padding(.horizontal, 16)
    }
}
```

Verify that `Color(hex:)` initializer exists in Constants.swift or Extensions.swift. If it's `Color.init(hex:)`, use that. If the project uses a different hex color initializer pattern, match it.

**In `JobHarvest/Models/Job.swift`**, add Equatable conformance to `JobFilters`:

Find `struct JobFilters: Codable {` and change it to `struct JobFilters: Codable, Equatable {`. Since all properties are optional primitives (`String?`, `[String]?`, `Double?`, `Bool?`), synthesized Equatable conformance works automatically -- no custom `==` implementation needed.

**Add ErrorBannerView.swift to the Xcode project:** After creating the file, it must be added to the Xcode project's file references. Use the same directory group as the other shared views (`JobHarvest/Views/Shared/`). If the project uses automatic file discovery (folder references), no additional step is needed. Otherwise, the file needs to be added to the `.xcodeproj` build sources.
  </action>
  <verify>
    <automated>cd "/Users/williamwriter/Desktop/Jake's Project/FlashApply-iOS" && xcodebuild build -project JobHarvest/JobHarvest.xcodeproj -scheme JobHarvest -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ErrorBannerView.swift exists at JobHarvest/Views/Shared/ErrorBannerView.swift
    - ErrorBannerView.swift contains `struct ErrorBannerView: View`
    - ErrorBannerView.swift contains `var onRetry: (() -> Void)?`
    - ErrorBannerView.swift contains `exclamationmark.triangle.fill`
    - ErrorBannerView.swift contains `e74c3c`
    - Job.swift contains `struct JobFilters: Codable, Equatable`
    - Build succeeds with zero errors
  </acceptance_criteria>
  <done>ErrorBannerView is a reusable inline error banner with optional retry. JobFilters conforms to Equatable enabling `currentFilters != JobFilters()` comparison for context-aware empty deck.</done>
</task>

</tasks>

<verification>
- Build succeeds: `xcodebuild build -project JobHarvest/JobHarvest.xcodeproj -scheme JobHarvest -destination 'platform=iOS Simulator,name=iPhone 16'`
- `grep -c "humanReadableDescription" JobHarvest/Utils/Extensions.swift` returns >= 1
- `grep -c "ErrorBannerView" JobHarvest/Views/Shared/ErrorBannerView.swift` returns >= 1
- `grep "Equatable" JobHarvest/Models/Job.swift` shows JobFilters with Equatable
</verification>

<success_criteria>
- Error.humanReadableDescription extension exists and maps all 8 raw patterns from the UI-SPEC error mapping table
- LoadingView shows branded bolt pulse animation (not generic ProgressView)
- ErrorBannerView component exists with message + optional retry
- JobFilters: Equatable compiles without manual == implementation
- Full project builds clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-polish/03-01-SUMMARY.md`
</output>
