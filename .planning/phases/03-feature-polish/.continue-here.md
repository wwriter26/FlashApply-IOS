---
phase: 03-feature-polish
task: checkpoint-fixes
total_tasks: human-verification
status: in_progress
last_updated: 2026-03-31T20:41:53.486Z
---

<current_state>
Phase 3 plans 01-05 are all executed. We're in the human verification checkpoint fixing issues the user found during testing. All 5 plans completed successfully with atomic commits. The verification report (03-VERIFICATION.md) shows 27/27 automated checks passed, 8 items need human testing.

The user has been testing on the simulator and we've been fixing issues iteratively. Several fixes are committed, several more are in uncommitted working state.
</current_state>

<completed_work>

- Plan 03-01: UX foundations (error humanization, branded loading, ErrorBannerView, JobFilters Equatable) — DONE
- Plan 03-02: Swipe card polish (animation bug fix, isGreatFit badge, context-aware empty deck) — DONE
- Plan 03-03: Payments + Profile (plan bridge, Stripe return detection, profile save feedback) — DONE
- Plan 03-04: Applied Jobs pipeline (empty state fix, stage move buttons, active/archived toggle) — DONE
- Plan 03-05 Task 1: Error humanization sweep across remaining tabs — DONE
- Plan 03-05 Task 2: Human verification checkpoint — IN PROGRESS

**Checkpoint fixes completed and committed (91859be):**
- NetworkService: separated 403 from 401 (swipe limit 403s were being treated as auth errors)
- SwipeResponse + UserProfile: added `swipesLeftToday` and `enduringSwipes` fields matching backend
- JobCardsViewModel: tracks both daily/enduring counts, blocks swiping when both are 0
- MyJobsView: renamed "All" tab to "Archived" (shows archived+failed only), alphabetical sort
- AuthorizationsSection: added labels above each segmented picker (duplicate US Work Auth fix)
- PremiumView: case-insensitive plan matching, current plan banner at top

**Additional fixes (uncommitted working state):**
- SubscriptionPlan model rewritten to match backend MembershipPlan enum (monthly-plus, seasonal-pro, etc.)
- PremiumView: app-to-web Stripe checkout (opens web app pricing page in Safari)
- Apply page badge: changed from DS/ES swipe counts to plan name with JobHarvest logo
- Settings page: added Swipe Balance section with DS/ES counts + Current Plan display
- PremiumView + ApplyView: replaced bolt.fill icons with jobHarvestTransparent logo asset
- Fixed double-https:// bug in webAppDomain URL construction
</completed_work>

<remaining_work>

- Verify the Stripe app-to-web checkout actually loads (was showing blank — fixed double-https, needs re-test)
- User wants to verify: swipe fly-off animation, profile save banner, stage move optimistic update, network error messages
- Once all human verification items pass → mark Phase 3 complete → update ROADMAP.md → move to Phase 4
- Commit all uncommitted checkpoint fixes
</remaining_work>

<decisions_made>

- Backend uses Stripe `ui_mode: 'embedded'` returning `client_secret`, not a URL — iOS can't use this directly. Decision: app-to-web pattern, open `https://dev.jobharvest.com/dashboard/premium` in SFSafariViewController
- Backend MembershipPlan enum values are `monthly-plus`, `seasonal-pro`, `lifetime-plus` etc — not just `plus`/`pro`. SubscriptionPlan model rewritten to match
- Apply page badge shows plan name (e.g., "Free") with logo instead of DS/ES swipe counts — swipe counts moved to More/Settings page
- User profile response includes `swipesLeftToday` and `enduringSwipes` fields — added to UserProfile model (decode-only, never sent back)
- MyJobsView "All" tab renamed to "Archived" showing only archived+failed stages. Active tab sorts alphabetically by job title
</decisions_made>

<blockers>
- Stripe checkout: the web app at dev.jobharvest.com/dashboard/premium requires web authentication — the SFSafariViewController won't share the iOS app's Cognito session. May need to pass an auth token as a URL parameter or use a dedicated checkout landing page that accepts a token. This needs investigation.
</blockers>

<context>
Phase 3 execution is 95% done. All code changes are made. The remaining work is: (1) commit the uncommitted fixes, (2) verify the Stripe web checkout actually works with auth, (3) quick simulator re-test of the remaining items, (4) mark phase complete and move to Phase 4: Hardening.

The user (Jake) is hands-on, testing on the simulator, and gives direct feedback. He prefers practical solutions over perfect ones. He was considering Apple IAP but deferred it — Stripe web checkout is the current path.
</context>

<next_action>
Start with: `git status` to see uncommitted changes, then `git add` and commit the checkpoint fixes. Then re-test the Stripe checkout URL in Safari to verify it loads correctly. If the web app requires auth, investigate passing the Cognito token or creating a lightweight checkout landing page.
</next_action>
