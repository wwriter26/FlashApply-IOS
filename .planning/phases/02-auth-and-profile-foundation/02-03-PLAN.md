---
phase: 02-auth-and-profile-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04
  - ONBD-05

must_haves:
  truths:
    - "Full auth + onboarding flow works end-to-end on simulator"
    - "Sign-out clears state and sign-in restores fresh data"
    - "Profile data displays correctly after fetch"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete auth routing, onboarding, and profile data flow against the live dev backend on the iOS Simulator.

Purpose: AUTH-01 through AUTH-04 and ONBD-01/ONBD-03 require a live Cognito session and backend API — they cannot be verified by build alone. This checkpoint confirms everything works end-to-end.

Output: Human confirmation that all auth and onboarding flows work, or a list of issues to address.
</objective>

<execution_context>
@/Users/williamwriter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/williamwriter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth-and-profile-foundation/02-01-SUMMARY.md
@.planning/phases/02-auth-and-profile-foundation/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Complete auth and onboarding flow on simulator</name>
  <files>N/A — verification only, no files modified</files>
  <read_first>
    .planning/phases/02-auth-and-profile-foundation/02-01-SUMMARY.md,
    .planning/phases/02-auth-and-profile-foundation/02-02-SUMMARY.md
  </read_first>
  <action>
    Present the user with the verification checklist below. Build and run the app on the simulator so it is ready for the user to test. Then pause for human verification.

    Prerequisites: Ensure `amplifyconfiguration.json` and `Config.xcconfig` are configured for the dev environment.

    **Test 1: New user onboarding (AUTH-02, ONBD-01 through ONBD-05)**
    1. Build and run on iPhone 16 Simulator
    2. Sign up with a new test account (or use a test account that has never completed onboarding)
    3. Verify: You see the onboarding quiz (not main tabs)
    4. Step 1: Upload a PDF resume. Verify "Next" button is enabled only after file selection.
    5. Step 2: Enter first + last name. Verify "Next" is disabled until both fields filled.
    6. Step 3: Phone number (optional — "Next" should be enabled even if empty)
    7. Step 4: Work authorization selection
    8. Step 5: Select job types AND select 2-3 skill tags from the tag picker. Verify tags highlight when tapped.
    9. Tap "Get Started". Verify "Getting things ready..." loading screen appears briefly.
    10. Verify: You land on MainTabView (5 tabs visible).

    **Test 2: Profile data populated (Codable fix)**
    11. Go to Profile tab. Verify: Name, phone, work auth, skills, resume filename are all displayed (not blank).
    12. If profile data is blank, check Xcode console for decode errors.

    **Test 3: Returning user (AUTH-01, AUTH-03, AUTH-04)**
    13. Kill the app (Cmd+Shift+H, then swipe up in app switcher, or just Cmd+Q).
    14. Relaunch the app.
    15. Verify: You go directly to MainTabView WITHOUT seeing onboarding or sign-in.
    16. Verify: Profile tab still shows your data.

    **Test 4: Sign-out and cleanup (AUTH-05)**
    17. Navigate to More tab > Settings > Sign Out (or wherever sign-out lives).
    18. Verify: Confirmation alert appears. If no alert, just verify sign-out triggers.
    19. Confirm sign-out.
    20. Verify: You land on the sign-in screen.
    21. Sign back in with the same account.
    22. Verify: You go to MainTabView (not onboarding — you already completed it).
    23. Verify: Profile tab shows fresh data from the backend.

    **Test 5: Quiz persistence on background (ONBD-04)**
    24. Sign out again. Sign up with a NEW test account.
    25. In the onboarding quiz, fill in steps 1-3 (resume, name, phone).
    26. Press Home (Cmd+Shift+H) to background the app.
    27. Re-open the app.
    28. Verify: Quiz is on the same step you left it, with previously entered data still filled in.
  </action>
  <verify>
    <automated>cd "/Users/williamwriter/Desktop/Jake's Project/FlashApply-iOS" && xcodebuild build -project JobHarvest/JobHarvest.xcodeproj -scheme JobHarvest -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - New user sees onboarding quiz after sign-up
    - Skills tag picker is visible and tappable on step 5
    - "Get Started" saves data and transitions to main tabs
    - Profile tab displays the data entered during onboarding
    - App restart preserves session (no re-login needed)
    - Sign-out returns to sign-in screen
    - Re-sign-in shows main tabs (not onboarding again)
    - Quiz progress is preserved on app background
  </acceptance_criteria>
  <done>Human confirms all 5 test scenarios pass, or provides a list of failures for gap closure.</done>
</task>

</tasks>

<verification>
Human completes the 5 test scenarios above and reports results.
</verification>

<success_criteria>
All 5 test scenarios pass: new user onboarding, profile data display, returning user session, sign-out cleanup, and quiz persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-and-profile-foundation/02-03-SUMMARY.md`
</output>
